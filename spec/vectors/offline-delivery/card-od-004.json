{
  "id": "card-od-004",
  "kind": "agent-card-extension",
  "description": "params.mode always_on is rejected (only sleep | hourly_wake).",
  "requires": [
    "agent-card",
    "offline-delivery-extension"
  ],
  "extension": {
    "uri": "https://atom.qwixl.dev/a2a/offline-delivery/v1",
    "description": "Offline delivery profile",
    "required": false,
    "params": {
      "mode": "always_on"
    }
  },
  "expect": {
    "schemaOk": false
  }
}
