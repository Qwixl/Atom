{
  "name": "atom",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @atom/shell-app dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
