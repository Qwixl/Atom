# {{MODULE_ID}}

Starter module generated by `atom-registry scaffold`.

1. Edit `bundle/index.html` — keep the postMessage bridge (`ready`, `event`).
2. Update `manifest.json` (`agentHint`, `semanticRole`, `events`).
3. Place the bundle on your static host under `bundleUrl`.
4. Run `atom-registry publish` to set integrity hashes and index entry.

See [MODULES.md](../../../MODULES.md) in the Atom repo.
